preuorder
symbol name
terminal/nonterminal
type expression if non-leaf -> sub-expression
name of lexeme if leaf node
line number if leaf node
grammar rule if non leaf
depth of node root ki depth 0

type expression 4 fields
